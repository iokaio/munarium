# SPDX-License-Identifier: Apache-2.0
[package]
name = "munarium-runbooks"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Runbook definitions + the checkpointed step machine's types. Executions are durable (runbook_runs/steps tables) and every transition can be a ledger event; the executor stays a step machine — fan-out orchestration is Temporal's seam, not scope creep here."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml = { workspace = true }
