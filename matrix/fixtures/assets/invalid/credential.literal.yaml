apiVersion: munarium.ioka.io/v1
kind: DataSource
metadata: { name: literalsecret, version: 1 }
spec:
  adapter: postgres
  credentialRef: "postgres://user:hunter2@db.example.com:5432/crm?sslmode=require"
  egress: { allowHosts: [db.example.com] }
  authorization: { strategy: source_native }
