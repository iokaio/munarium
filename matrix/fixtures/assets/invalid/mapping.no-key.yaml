apiVersion: munarium.ioka.io/v1
kind: ClaimMapping
metadata: { name: mappingnokey, version: 1 }
spec:
  source: crm
  entity:
    table: holdings
    key: []
    subjectTemplate: "shareholder.holder"
  properties:
    shares: { column: shares, type: decimal, scale: 0 }
  temporal:
    validTime: { column: effective_date }
