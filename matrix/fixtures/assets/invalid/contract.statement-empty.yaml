apiVersion: munarium.ioka.io/v1
kind: QueryContract
metadata: { name: emptystatement, version: 1 }
spec:
  source: crm
  statementByDialect:
    postgres: {}
  result:
    columns:
      region: { type: string, key: true }
    columnOrder: [region]
