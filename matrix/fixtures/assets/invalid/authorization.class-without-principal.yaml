apiVersion: munarium.ioka.io/v1
kind: DataSource
metadata: { name: noprincipal, version: 1 }
spec:
  adapter: landing
  egress: { allowHosts: [blob.core.windows.net] }
  authorization:
    strategy: per_class_principals
    classes:
      - { name: sales, compartments: [sales], accessLevel: 1 }
