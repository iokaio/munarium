apiVersion: munarium.ioka.io/v1
kind: DataSource
metadata: { name: keyoutside, version: 1 }
spec:
  adapter: postgres
  egress: { allowHosts: [db.example.com] }
  authorization: { strategy: source_native }
  sync:
    mode: snapshot
    entity: { table: t, key: [id] }
    projection: [name]
