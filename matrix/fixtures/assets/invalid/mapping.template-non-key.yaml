apiVersion: munarium.ioka.io/v1
kind: ClaimMapping
metadata: { name: mutablesubject, version: 1 }
spec:
  source: crm
  entity:
    table: holdings
    key: [holder_id]
    subjectTemplate: "shareholder.{holder_name}"
  properties:
    shares: { column: shares, type: decimal, scale: 0 }
  temporal:
    validTime: { column: effective_date }
