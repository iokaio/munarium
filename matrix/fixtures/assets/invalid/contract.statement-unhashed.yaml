apiVersion: munarium.ioka.io/v1
kind: QueryContract
metadata: { name: unhashed, version: 1 }
spec:
  source: crm
  statementByDialect:
    postgres: { file: query/open-pipeline.pg.sql }
  result:
    columns:
      region: { type: string, key: true }
    columnOrder: [region]
