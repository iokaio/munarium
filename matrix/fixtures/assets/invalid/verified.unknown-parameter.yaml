apiVersion: munarium.ioka.io/v1
kind: QueryContract
metadata: { name: unknownparam, version: 1 }
spec:
  source: crm
  statementByDialect: { postgres: { inline: "SELECT region FROM opportunities" } }
  result:
    columns:
      region: { type: string, key: true }
    columnOrder: [region]
  verifiedQuestions:
    - question: "regions?"
      parameters: { as_of: "2026-06-30" }
      expect: { rows: 3 }
