# Ownership of Munarium. Only a code owner's review satisfies the pull-request gate on
# main; anyone else's approval is a welcome comment, not the gate.
# The protected files (CONTRIBUTING.md, "Protected files") change only through the owner.
#
# One rule for the whole repository. Split this when a component gets a second
# maintainer, not before.
*   @tylerje
